# 小组件优化验证记录

日期：2026-09-20。基于用户提交 `a76e8da`，保留 Material 配置页、字体选择及圆角横向留白。

## 本次改动

- 小组件不显示时区名称、不单独选择时区；数字时钟、指针、日期、农历和午夜刷新统一跟随 App 时区。App 修改时区后自动更新已有实例；空时区设置表示跟随系统。旧 JSON 字段保留兼容读取。
- 固定字体 ID 与资源列的映射，避免字体目录排序改变后选错字体。
- 合并预览滑块的连续请求，跳过过期任务；修复配置页重建时草稿和控件恢复状态的竞争。
- 按宽矮、窄高和大号尺寸调整天气与日期布局，控制主数字高度，确保最大字号下底部文字完整；无缓存天气优先显示可操作的提示。
- 响应式布局共用一次时间与天气快照；去除重复节日名称。
- 修复设备测试将已解析的像素尺寸再次乘密度的问题；新增所有可见文字的实际行高检查，避免仅检查控件边界而遗漏字形裁切。

## 实际执行

环境：Android 15 / API 35，MuMu `emulator-5554`，Lawnchair；使用项目既有签名。

| 检查 | 结果 |
| --- | --- |
| `testUltimateDebugUnitTest` | 190 项通过，无失败、错误或跳过 |
| `lintUltimateDebug` | 0 错误；项目仍有 lint 警告 |
| `assembleUltimateDebug`、`assembleUltimateDebugAndroidTest` | 成功 |
| 设备验收 | 21,635 项检查通过，1,800 次真实 RemoteViews 渲染 |
| 已有时钟跟随 App 时区 | 夏威夷、东京、跟随系统三次切换均自动刷新 |
| 在线天气 | 使用设备已有 QWeather 配置，真实刷新成功 |
| 截图复核 | 复核纸张、深色主题及默认／Bitcount 字体，确认底部文字完整、无时区名称 |

渲染矩阵包括 4 类小组件、6 套主题、5 档尺寸、12 个字体选项；另覆盖真实最小尺寸、简体中文／繁体中文／英文、85%／120% 字号、12 小时制含秒，以及天气有缓存／无缓存状态。

节日与天气布局使用 2026-10-01 和固定天气样本，不依赖当天是否有节日；在线天气检查是单独的真实请求。TextClock 仍读取设备当前时间。

同时检查四个选择器入口、系统绑定、实例隔离、配置取消不写入、完成保存、无天气实例时取消周期任务，以及测试绑定清理。

复现命令见 [WIDGET_CONTRACT.md](WIDGET_CONTRACT.md)。本轮未重新执行重启、系统语言切换、壁纸／夜间模式切换以及长期后台耗电的完整人工矩阵；上述结果不代表这些场景已全部验收。

## 设备恢复与清理

- 恢复基准为用户本次修改后的设备快照，而非前一天的旧版本。
- 恢复原 APK，校验文件 SHA-256 一致；测试包从用户基准提交重新构建并恢复。
- 恢复应用私有数据，启动前逐文件哈希与备份一致。原版启动后按自身逻辑清除了一个已解绑实例 `widget_17` 的遗留记录；有效实例配置和 App 设置保持一致。
- 原有三个 ClockMods 小组件绑定及其他桌面绑定、已安装包集合、system/global/secure 设置均与快照一致；前台回到 Lawnchair，设备保持唤醒。
- 清理本任务的本地临时目录、备份、截图及设备临时 UI 文件。保留最终 APK、标准构建报告和已有缓存；不清理用户原有临时目录。
