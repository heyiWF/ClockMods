<?xml version="1.0" encoding="utf-8"?>
<!--
  The dial is the whole content and it wants a square, so the default stays 2x2: about 100-140dp
  across on a phone, which reads clearly. The resize floor is raised from 80dp to 110dp because
  below that the two corner insets leave a dial too small to read the hands.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
 android:minWidth="110dp" android:minHeight="110dp"
 android:targetCellWidth="2" android:targetCellHeight="2"
 android:minResizeWidth="110dp" android:minResizeHeight="110dp" android:maxResizeWidth="600dp" android:maxResizeHeight="500dp"
 android:resizeMode="horizontal|vertical" android:widgetCategory="home_screen" android:widgetFeatures="reconfigurable|configuration_optional"
 android:updatePeriodMillis="0" android:initialLayout="@layout/widget_analog_medium" android:previewLayout="@layout/widget_analog_medium"
 android:description="@string/widget_analog_description" android:configure="com.clockmods.widget.config.WidgetConfigActivity"/>
