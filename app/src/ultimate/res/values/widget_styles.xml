<resources>
    <style name="Theme.ClockMods.Widget.Dynamic" parent="android:style/Theme.DeviceDefault.DayNight" />

    <!-- Compose owns the host surface; RemoteViews resolve their own theme. -->
    <style name="Theme.ClockMods.WidgetConfig" parent="Theme.ClockMods">
        <item name="android:windowBackground">@color/clock_default_background</item>
    </style>
</resources>
